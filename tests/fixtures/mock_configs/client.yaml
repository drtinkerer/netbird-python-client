# Mock NetBird client configurations for testing

default_client:
  host: "api.netbird.io"
  api_token: "test-token-123"
  timeout: 30.0
  base_path: "/api"

test_client:
  host: "test.netbird.io"
  api_token: "test-token-456"
  timeout: 10.0
  base_path: "/api/v1"

local_client:
  host: "http://localhost:8080"
  api_token: "local-token"
  timeout: 5.0
  base_path: "/api"

# Environment configurations
environments:
  development:
    host: "dev.netbird.io"
    timeout: 30.0
  
  staging:
    host: "staging.netbird.io"
    timeout: 15.0
  
  production:
    host: "api.netbird.io"
    timeout: 10.0